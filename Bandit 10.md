This is pretty straightforward we just need to decode it.
I went to a site and decoded it.

But there a command called base64 which is better.

echo ................................................... | base64 --decode.